export const debounceTimeout = 500;
// the maximum number of entries will be loaded in one request
export const loadSize = 50;
// the minimal length of the search input so a request will be fire
export const searchInputMinimalLength = 3;
